import { CoreumHeaders as _CoreumHeaders } from "@/internal/CoreumHeaders/CoreumHeaders";

/**
 * Headers is extended to include helpers and intellisense for common
 * header names.
 * */

export const CoreumHeaders = _CoreumHeaders;
export type CoreumHeaders = _CoreumHeaders;
export const Headers = _CoreumHeaders;
export type Headers = _CoreumHeaders;
