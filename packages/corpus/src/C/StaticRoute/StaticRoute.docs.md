# StaticRoute

Represents a route that serves a static file from disk.

## constructor

Creates a new static route bound to a file path and address.

## filePath

Absolute or relative path to the file served by this route.

# StaticRouteAbstract

Base class all static route implementations extend.
