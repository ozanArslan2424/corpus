import { CoreumRequest as _CoreumRequest } from "@/internal/CoreumRequest/CoreumRequest";

/**
 * Core.Request includes a cookie jar, better headers, and some utilities.
 * */

export const CoreumRequest = _CoreumRequest;
export type CoreumRequest = _CoreumRequest;
export const Request = _CoreumRequest;
export type Request = _CoreumRequest;
